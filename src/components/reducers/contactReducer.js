/**
 * User: clint
 * Date: 18/09/2018
 * Time: 20:34
 *
 * Milner.io
 */

const initialState = {
    name: 'Clint'
};

export default (state = initialState, action) => {
    switch(action.type) {
        default: return state;
    }
}